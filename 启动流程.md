# 本地开发启动流程

## 固定端口

- 前端 Vite：`http://127.0.0.1:5173`
- 后端 FastAPI：`http://127.0.0.1:8765`
- Vite 将 `/api` 和 `/health` 代理到 `http://127.0.0.1:8765`

本机的 `8000` 和 `8001` 可能被其他版本的后端占用，不要用于本项目，也不要为了启动本项目而终止来源不明的进程。

## 首次初始化

在项目根目录执行。已有 `.env` 时必须保留，不要覆盖。

```powershell
# 仅在 .env 不存在时创建
if (-not (Test-Path .env)) { Copy-Item .env.example .env }

# 后端虚拟环境和依赖
if (-not (Test-Path backend\.venv\Scripts\python.exe)) {
  python -m venv backend\.venv
}
backend\.venv\Scripts\python.exe -m pip install -e "backend[dev]"

# 前端依赖
Set-Location frontend
npm install
Set-Location ..

# 本地 SQLite 数据库、迁移和演示数据
Set-Location backend
$env:DATABASE_URL = "sqlite+pysqlite:///dev.sqlite3"
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m app.scripts.seed_demo
Set-Location ..
```

以后日常启动不需要重复安装依赖。迁移和演示数据写入命令可安全重复执行。

## 启动前检查

在项目根目录检查固定端口：

```powershell
Get-NetTCPConnection -LocalPort 5173,8765 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,OwningProcess
```

无输出表示端口空闲。如果有输出，先用下面的命令确认进程属于本项目：

```powershell
$pidToInspect = <OwningProcess>
Get-CimInstance Win32_Process -Filter "ProcessId = $pidToInspect" |
  Select-Object ProcessId,ParentProcessId,CommandLine
```

只有命令行明确包含当前项目路径，并且是 `vite`、`uvicorn` 或 `run_ai_worker` 时，才可以停止该进程树：

```powershell
taskkill.exe /PID <OwningProcess> /T /F
```

不要终止来源不明的进程，不要清理 `8000` 或 `8001`。

## 日常启动

打开三个 PowerShell 终端，分别执行以下命令。

### 终端 1：后端 API

```powershell
Set-Location backend
$env:DATABASE_URL = "sqlite+pysqlite:///dev.sqlite3"
.\.venv\Scripts\python.exe -m uvicorn app.main:app `
  --reload `
  --host 127.0.0.1 `
  --port 8765
```

### 终端 2：AI Worker

```powershell
Set-Location backend
$env:DATABASE_URL = "sqlite+pysqlite:///dev.sqlite3"
.\.venv\Scripts\python.exe -m app.scripts.run_ai_worker
```

Worker 只能启动一个实例。重复启动会造成同一任务被多个进程竞争处理。

### 终端 3：前端 Vite

```powershell
Set-Location frontend
$env:VITE_API_PROXY_TARGET = "http://127.0.0.1:8765"
npm run dev -- --config vite.config.ts --host 127.0.0.1 --port 5173
```

必须显式传入 `--config vite.config.ts`，避免 Vite 误用本地残留的生成配置文件。`VITE_API_PROXY_TARGET` 只在 Vite 启动时读取，修改后需要重启 Vite。

## 启动验收

在第四个 PowerShell 终端执行：

```powershell
$live = Invoke-RestMethod http://127.0.0.1:8765/health/live
$ready = Invoke-RestMethod http://127.0.0.1:8765/health/ready
$proxyReady = Invoke-RestMethod http://127.0.0.1:5173/health/ready
$openapi = Invoke-RestMethod http://127.0.0.1:8765/openapi.json
$growthRegistered = $openapi.paths.PSObject.Properties.Name -contains "/api/v1/growth"

$live
$ready
$proxyReady
"growth route registered: $growthRegistered"
```

正确结果应满足：

- 三个健康检查均显示 `ok` 或 `ready`。
- `growth route registered: True`。
- 打开 `http://127.0.0.1:5173` 后可以登录并访问“思考成长”。
- 后端接口文档位于 `http://127.0.0.1:8765/docs`。

如果前端 `/api/v1/growth` 返回 `404`，先检查 Vite 终端是否设置了 `VITE_API_PROXY_TARGET=http://127.0.0.1:8765`，并确认 Vite 使用的是 `vite.config.ts`。不要通过反复启动更多后端实例解决端口问题。

## 停止服务

正常情况下，在三个运行终端中分别按 `Ctrl+C`。停止后重新执行端口检查，确保 `5173` 和 `8765` 没有监听。

如果终端已经关闭但服务仍在运行，按“启动前检查”中的方法核实命令行，只终止属于当前项目的进程树。

## 发给 Codex 的启动指令

新会话中可以直接发送以下内容：


```text
请阅读 README.md、启动流程.md 和项目配置，然后实际启动当前项目，不要只给出步骤。保留已有 .env。按照启动流程.md 使用 SQLite 开发库，在 127.0.0.1:8765 启动 FastAPI，在单独进程启动一个 AI Worker，并在 127.0.0.1:5173 启动 Vite。启动 Vite 时设置 VITE_API_PROXY_TARGET=http://127.0.0.1:8765，并显式使用 --config vite.config.ts。启动前只检查和清理属于当前项目的 5173/8765 旧进程。启动后实际验证 /health/live、/health/ready、前端代理 /health/ready、OpenAPI 中的 /api/v1/growth。保持服务运行，并给出可点击的前端地址。
```
