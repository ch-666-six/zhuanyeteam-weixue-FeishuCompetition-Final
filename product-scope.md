# 思辨表达 AI 助教：产品范围

> 状态：新项目开发基线  
> 更新日期：2026-08-14  
> 关联文档：[会话状态](./session-state.md) · [架构决策](./architecture-decisions.md)

## 1. 产品目标

本产品面向 1-7 年级学生，帮助学生在不替代独立思考的前提下完成观点表达、论证梳理和表达改进。

第一版只验证一个核心闭环：

```text
选择作业 -> 独立文字初答 -> 查看 AI 初析 -> 独立提交终答 -> 查看 AI 辅助评价
```

AI 的职责是识别学生已经表达的内容、提出可理解的改进方向并提供形成性反馈。AI 不代写最终答案，不替代教师评价，不输出班级排名或自动定论。

## 2. 目标用户与首要场景

### 2.1 学生

- 使用演示身份进入对应年级。
- 查看已发布且属于本年级的作业。
- 在截止时间前完成文字初答和终答。
- 查看 AI 对论证结构的初析和对最终答案的辅助评价。
- 刷新页面、重新登录或重复点击后，仍能回到正确步骤且不会重复提交。

### 2.2 教师

MVP 不提供教师端界面。作业由种子数据或管理脚本创建。教师评价、班级管理和正式账号体系不属于 MVP。

## 3. 产品原则

1. **学生作品优先**：AI 内容与学生原文必须清晰区分，不能把 AI 建议伪装成学生答案。
2. **一次一个动作**：页面始终明确当前步骤、当前可执行操作以及提交后的结果。
3. **失败可恢复**：AI 超时或失败不丢失学生答案，也不让会话进入无法继续的状态。
4. **服务端定流程**：页面显示和跳转以服务端会话快照为准，前端不自行推导业务状态。
5. **适龄但不幼稚**：语言简短、具体，避免技术术语、成人化评价和过度游戏化。
6. **评价不等于评分**：首版使用维度反馈和证据，不展示综合分、排名或能力定性。

## 4. MVP 范围

### 4.1 身份与作业

- 提供 1-7 年级演示学生身份，可切换年级。
- 作业包含题目、可选材料、作答要求、年级、发布时间和截止时间。
- 学生只能读取所属年级的已发布作业。
- 作业列表显示待开始、进行中、评价处理中、已完成和已截止状态。

### 4.2 文字初答

- 学生可以创建一条作答会话；同一学生与作业只能有一条进行中的会话。
- 支持纯文字输入、字数提示、本地草稿和明确的提交确认。
- 草稿键至少包含学生 ID、作业 ID、会话 ID 和阶段。
- 重复创建或重复提交不能产生多个有效会话或重复 AI 任务。

### 4.3 AI 初始分析

- 分析学生原文中的观点、理由、证据、反方观点、回应和条件/边界。
- 只展示在原文中确有依据的元素；引用必须能定位到原文。
- 最多突出一个优先改进方向。
- AI 分析通过持久化任务执行，页面可以安全轮询状态。
- AI 失败时保留初答，允许重试分析；失败不会自动改写学生内容。

### 4.4 最终答案

- 学生查看初析后独立编辑和提交最终答案。
- 终答编辑器不自动写入 AI 生成的完整答案。
- 每次提交形成不可变的答案版本；MVP 只允许产生首个版本。
- 截止后不能新建会话或提交答案。

### 4.5 AI 辅助评价

- 最终提交后创建评价任务，页面展示排队、处理中、失败和完成状态。
- 评价覆盖立意、选材、结构、语言和视角五个维度。
- 每个结论尽量关联学生原文证据，并给出一项优先改进建议。
- 评价失败可重试，重试仍绑定同一答案版本，不重复创建最终答案。
- 模型结果必须通过结构、证据和安全校验后才能展示。

### 4.6 基础体验与安全

- 支持 360、768、1280 和 1440 像素宽度，无关键内容遮挡和水平滚动。
- 键盘可完成完整文字流程；异步状态和表单错误可被辅助技术识别。
- 不依赖远程字体完成正常展示。
- 密钥仅保存在后端；日志默认不记录完整学生正文。

## 5. MVP 明确不做

- AI 多轮辅导、19+1 轮和独立复述。
- 已提交答案的直接修改或继续与 AI 交流。
- 录音、语音转写和音频文件管理。
- 教师端、教师评价、班级管理和正式账号注册。
- 成长档案、跨作业趋势、推荐算法和能力排名。
- 实时推送；MVP 使用有限频率的状态轮询。
- 多 AI 提供商自动路由、复杂 Prompt 管理平台或通用插件系统。
- 微服务、事件总线和 Kubernetes 等分布式基础设施。

## 6. 后续增量

后续功能只有在前一增量通过验收后才进入开发。

| 增量 | 新增能力 | 依赖前提 |
|---|---|---|
| V1.1 | AI 多轮辅导、提前结束、19+1 轮、独立复述 | MVP 会话和任务恢复稳定 |
| V1.2 | 答案版本修改、直接修改、继续辅导、取消修订 | 不可变提交版本及评价绑定稳定 |
| V1.3 | 语音录入、转写确认、音频清理 | 文字链路稳定且文件存储边界明确 |
| V1.4 | 教师评价与学生结果整合 | 教师身份和权限模型明确 |
| V1.5 | 成长档案与跨作业证据趋势 | 有足够真实样本并完成指标定义 |

### 6.1 V1.1 文字辅导契约

V1.1 在既有文字闭环中加入可选的 AI 启发式辅导，不包含录音、语音转写或音频存储：

```text
初答 -> 初析与首问 -> 跳过辅导或开始辅导 -> 最多 20 轮问答 -> 独立终答 -> 五维评价
```

- 一轮由一个 AI 问题和一次学生回答组成；初析中的首问计为第 1 轮。
- 学生可以在首问或任意后续轮次结束辅导并进入终答。
- 第 20 次回答保存后自动结束，不生成第 21 个问题。
- AI 每轮只提出一个问题，不生成或改写学生的完整答案。
- 最终评价仍以题目、初答和不可变终答为输入，V1.1 不以辅导记录作为评价依据。
- 辅导问题只有通过结构与安全校验后才可展示；展示使用受权流式接口。
- 截止后可以读取已有记录，但不能新增辅导回答或提交终答。

每个增量都必须补充对应的状态、允许动作、幂等规则、失败恢复和验收测试，不能只增加页面分支。

## 7. MVP 页面

| 页面 | 核心内容 | 主操作 |
|---|---|---|
| 演示登录 | 年级身份选择 | 进入作业列表 |
| 作业列表 | 状态、截止时间、继续位置 | 开始或继续 |
| 作业详情 | 题目、材料、要求 | 开始作答 |
| 初答 | 学生编辑器、草稿状态、字数 | 提交初答 |
| 分析等待 | AI 任务状态、可恢复错误 | 重试或继续查看 |
| 初析 | 原文证据、结构元素、优先建议 | 编写最终答案 |
| 终答 | 独立编辑器、提交确认 | 提交最终答案 |
| 评价等待 | 评价任务进度 | 重试或稍后查看 |
| 结果 | 摘要、优点、下一步、五维证据 | 返回作业列表 |

## 8. MVP 验收场景

以下场景必须在 Mock AI 下自动化通过：

1. 学生从作业列表完成初答、初析、终答和评价的完整流程。
2. 初析和评价分别发生超时、非法 JSON 与可重试错误时，答案不丢失且可恢复。
3. 创建会话、提交初答和提交终答被重复点击时，只产生一个有效业务结果。
4. 页面刷新、直接访问旧 URL 和重新登录后，服务端返回的下一视图一致。
5. 两个标签页同时提交时，旧版本请求收到明确冲突，不覆盖新数据。
6. 学生不能读取其他学生的会话，也不能访问其他年级未授权作业。
7. 截止时间边界使用服务端时间判断，截止后写操作被拒绝。
8. 360 和 1280 像素视口可完成全流程，键盘操作无阻塞。

## 9. MVP 成功标准

- 完整文字流程在 Mock AI 环境稳定通过端到端测试。
- 所有写接口具有权限、动作许可、幂等和版本冲突测试。
- AI 服务中断并重启后，排队或运行中的任务可以恢复或明确失败。
- 非法模型输出不会进入可展示的领域结果表。
- 任一会话仅凭 URL 和服务端快照即可恢复，不依赖浏览器临时状态决定流程。
- 常规 CI 不访问真实 DeepSeek 或语音服务。

产品效果指标应在试用后另行确定。没有真实使用数据前，不以 AI 分数提升或模型生成量作为产品成功指标。
