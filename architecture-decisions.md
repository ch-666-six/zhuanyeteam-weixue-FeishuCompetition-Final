# 思辨表达 AI 助教：架构决策

> 状态：新项目初始决策集  
> 更新日期：2026-08-14  
> 关联文档：[产品范围](./product-scope.md) · [会话状态](./session-state.md)

## 1. 使用方式

本文记录新项目第一阶段已经确定的架构选择。每项决策包含背景、选择和后果。若以后改变选择，应新增决策记录并标记被替代关系，而不是无说明地修改实现。

## ADR-001：采用模块化单体

**状态**：接受

**背景**：产品仍在验证阶段，团队规模、访问量和独立扩容需求尚未确定。微服务会提前引入部署、网络、事务和观测成本。

**决策**：后端作为一个 FastAPI 应用部署，按 `assignments`、`sessions`、`submissions`、`evaluations` 等业务模块组织。模块通过应用用例和明确类型协作，不跨模块随意操作 ORM 对象。

**后果**：本地开发和事务处理更简单；模块边界仍需代码审查和测试约束。只有出现独立扩容、隔离或团队所有权需求时才评估拆分服务。

## ADR-002：按纵向功能切片交付

**状态**：接受

**背景**：先集中建设所有后端层、任务系统和 UI 基础，会长时间没有可验证的用户闭环，也容易在真实页面出现后推翻契约。

**决策**：按以下顺序交付，每个切片包含迁移、领域规则、API、前端和测试：

1. 工程骨架和演示身份。
2. 作业与文字初答。
3. AI 初始分析。
4. 最终答案与评价，完成 MVP。
5. 辅导、修订、语音、教师评价和成长档案分别作为后续切片。

**后果**：每个阶段均可演示和回归。共享抽象只在当前切片需要或已经出现稳定重复时建立。

## ADR-003：后端是流程唯一事实来源

**状态**：接受

**背景**：状态分别存在于数据库枚举、服务条件、前端路由和浏览器存储时，刷新、并发和版本升级会产生分叉。

**决策**：后端统一返回版本化 `SessionSnapshot`，其中包含 `phase`、`mode`、任务状态、`allowedActions` 和 `nextView`。所有写操作先经过领域动作许可和版本检查。详细规则见[会话状态](./session-state.md)。

**后果**：前端可以在任意刷新点恢复。后端不返回具体 URL，避免与 React Router 耦合；前端仅保留一处视图到路由的机械映射。

## ADR-004：保留熟悉的前后端技术栈

**状态**：接受

**决策**：

- 前端：React 19、TypeScript、Vite、React Router、TanStack Query。
- 后端：Python、FastAPI、Pydantic 2、SQLAlchemy 2、Alembic。
- 图标：Lucide React。
- 测试：Pytest、Vitest/Testing Library、Playwright。

**后果**：降低迁移和学习成本。Zustand 不作为默认依赖；只有出现无法由组件状态、URL 或 TanStack Query 合理承载的跨组件临时状态时再引入。

## ADR-005：生产目标数据库为 PostgreSQL

**状态**：接受

**背景**：持久化任务认领、并发写入、约束和 JSON 查询会很快触及 SQLite 的能力边界。

**决策**：生产和共享测试环境使用 PostgreSQL。SQLite 可用于无并发的本地快速测试，但不能成为唯一集成测试数据库。数据库变更全部由 Alembic 管理，应用启动不得静默执行 DDL。

**后果**：需要提供容器化本地数据库和 CI 服务。涉及锁、唯一约束和任务租约的测试必须在 PostgreSQL 上运行。

## ADR-006：采用应用用例、领域规则和基础设施三类边界

**状态**：接受

**决策**：建议结构如下：

```text
backend/app/
  api/                   HTTP、认证、响应模型
  modules/
    assignments/
    sessions/
    submissions/
    evaluations/
  ai/                    AI 端口、Schema、Prompt、Provider、运行记录
  jobs/                  持久化任务和 worker
  infrastructure/        数据库、日志、外部服务、文件存储
  shared/                配置、时钟、ID、通用错误
```

每个业务模块内部可以包含 `domain.py`、`application.py`、`models.py`、`repository.py` 和 `schemas.py`，但不要求为了形式创建空层。

**后果**：领域纯函数不依赖 FastAPI、具体 AI SDK 或 ORM Session。应用用例拥有事务边界。Repository 只在集中复杂查询、锁定和持久化规则时使用，不为每张表制造通用 CRUD 接口。

## ADR-007：学生提交不可变，修改产生新版本

**状态**：接受

**背景**：覆盖原答案会让历史评价无法复现，也会使取消修改和失败回滚变得模糊。

**决策**：最终答案保存在 `answer_submissions`，提交后不可修改。会话保存 `current_submission_id`。后续修订创建新版本，评价始终绑定特定提交版本。

**后果**：可以可靠回看、重试和对比。MVP 只产生首个版本，但数据模型不需要在加入修订功能时重做。

## ADR-008：所有模型工作通过持久化任务执行

**状态**：接受

**背景**：模型调用耗时且可能超时，进程内后台任务在重启时会丢失。

**决策**：初析和最终评价均先创建 `ai_jobs`，再由 worker 认领。任务具有逻辑唯一键、状态、尝试次数、租约、心跳、下次执行时间和错误码。演示环境可以让 Web 与 worker 同进程启动，但任务事实必须保存在数据库中。

**后果**：前端通过快照轮询任务状态，服务重启后可恢复。MVP 不引入 Redis/Celery；当数据库轮询成为性能瓶颈或需要大量独立 worker 时再决策队列产品。

## ADR-009：AI 是端口，不是业务层

**状态**：接受

**背景**：业务流程直接依赖 DeepSeek 的请求格式，会使测试、模型替换和失败降级困难。

**决策**：定义面向能力的端口，例如 `analyze_initial_answer()` 和 `evaluate_submission()`。提供确定性的 Mock Provider 和 DeepSeek Provider。业务层只接触版本化的输入输出模型，不接触厂商响应。

**后果**：常规测试完全离线，真实 API 只用于显式冒烟测试。暂不建设多供应商自动路由或通用插件框架。

## ADR-010：AI 输出经过分层验证并保留版本

**状态**：接受

**决策**：模型输出按以下顺序处理：

```text
厂商响应
  -> Pydantic Schema 解析
  -> 枚举、长度和范围校验
  -> 原文引用与业务规则校验
  -> 安全校验
  -> 领域结果持久化
```

每次调用记录 `provider`、`model`、`promptVersion`、`schemaVersion`、`rubricVersion`、输入摘要、耗时、尝试次数和终态。Prompt 以版本化文件保存；历史结果不因 Prompt 更新自动重算。

**后果**：模型返回合法 JSON 不代表业务合法。失败的原始响应可按受控策略用于诊断，但不能成为学生端结果，且不得在普通应用日志中输出完整正文。

## ADR-011：写接口统一使用幂等键与乐观锁

**状态**：接受

**决策**：所有有业务副作用的接口接收 `Idempotency-Key`；会话写入另接收 `expectedVersion`。幂等请求摘要、已提交响应和业务修改在同一事务内落库。版本更新使用数据库条件更新或 SQLAlchemy 版本列。

**后果**：重复点击和网络重试不会重复产生会话、提交或任务。并发标签页会收到显式 `409` 和最新快照，不发生最后写入者静默覆盖。

## ADR-012：API 契约先行并生成前端类型

**状态**：接受

**决策**：FastAPI 路由必须声明输入、成功响应和统一错误模型。CI 导出 OpenAPI，并生成或校验前端 TypeScript 类型。核心枚举和 DTO 不手工维护两份。

**后果**：契约变化会在构建阶段暴露。兼容性变更先发布后端，再发布前端；破坏性变化通过显式 API 或 Schema 版本处理。

## ADR-013：前端按业务功能组织，服务端状态归 TanStack Query

**状态**：接受

**决策**：

```text
frontend/src/
  app/                   Router、认证、QueryClient
  features/
    assignments/
    answer-session/
    evaluation/
  shared/
    api/                  HTTP、错误映射、生成类型
    ui/                   通用可访问组件
    styles/               tokens、base
```

TanStack Query 保存服务端状态。页面通过 feature hooks 执行动作、更新快照、失效关联查询并导航。浏览器存储只保存可丢弃草稿，不保存流程模式或权威状态。

**后果**：刷新恢复依赖服务端，组件不会各自复制缓存和路由规则。草稿键包含学生、作业、会话和阶段，身份切换时隔离。

## ADR-014：测试以 Mock AI 和真实数据库语义为基础

**状态**：接受

**决策**：

- 领域测试覆盖状态、动作、轮次和不变量。
- 应用测试覆盖事务、幂等、并发、任务创建和失败补偿。
- API 契约测试覆盖权限、错误码和 OpenAPI。
- AI 适配器测试使用固定厂商响应夹具。
- Playwright 覆盖 MVP 完整流程、刷新、重复点击和移动端。
- PostgreSQL 集成测试覆盖唯一约束、锁和任务租约。
- 真实 DeepSeek 测试只在显式环境变量开启时运行。

**后果**：常规 CI 可重复且不产生 API 成本。单纯 Mock 仓储测试不能替代关键数据库并发测试。

## ADR-015：隐私、日志和可观测性默认最小化

**状态**：接受

**决策**：贯通 `requestId`、`sessionId`、`jobId` 和 `aiRunId`；记录状态变化、耗时、错误码、重试次数、模型和版本。默认不记录完整学生正文、模型完整响应、令牌或密钥。需要诊断载荷时使用受控存储、环境开关、访问限制和保留周期。

**后果**：仍可追踪一条会话和 AI 任务，而不会把敏感内容扩散到普通日志系统。正式试点前需补充隐私告知、数据删除和保留政策。

## ADR-016：功能开关控制未完成增量

**状态**：接受

**决策**：辅导、修订、语音、教师评价和成长档案分别使用服务端功能开关。关闭时后端不返回对应动作，前端不展示入口。功能开关不能绕过权限、状态或数据迁移。

**后果**：功能可以按需叠加和逐步开放。避免在一个总开关中混合多项能力，也不依赖仅前端隐藏来控制访问。

## ADR-017：辅导记录结构化持久化并经校验后流式展示

状态：已接受（V1.1）。

- 辅导轮次使用独立关系表保存，不写入浏览器状态或初析 JSON。
- 每个后续问题仍通过持久化 `ai_job` 和独立 Worker 生成，HTTP 请求不直接调用模型。
- 模型完整输出先经过版本化 Schema 和安全规则校验，再写入轮次记录。
- 前端通过带鉴权的 `fetch` 读取 SSE；服务端将已验证问题分片发送，避免展示未校验 token。
- 断线重连可以重新读取同一已验证问题，不创建新的模型调用或辅导轮次。

## 2. 暂缓决策

以下选择尚无足够需求，不在 MVP 中提前决定：

- Redis、Celery 或其他专用消息队列。
- 对象存储和音频转码方案。
- 正式账号、学校租户和 RBAC 细节。
- 多模型自动路由、降级和成本优化策略。
- 实时 WebSocket/SSE 任务推送。
- 成长指标、推荐算法和教师分析模型。
- 服务拆分、容器编排和多区域部署。

当实际切片需要其中一项时，应补充新的 ADR，写明触发条件、备选方案、迁移和回滚影响。

## 3. 第一轮工程门禁

任何 MVP 切片合并前必须满足：

- 迁移可以从空 PostgreSQL 数据库执行。
- 新写接口具备权限、非法动作、幂等和版本冲突测试。
- API DTO 没有核心 `dict[str, Any]`，前端枚举类型与 OpenAPI 一致。
- AI 测试默认离线，非法输出不能进入领域结果。
- 页面刷新后可以通过最新快照恢复。
- 前后端测试、类型检查、构建和对应 E2E 全部通过。
- 日志中没有密钥和默认完整学生正文。
- 相关产品范围、状态规范或 ADR 已随行为变化同步更新。
